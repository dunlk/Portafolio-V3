---

const items = [
  {
    label: "About",
    href: "/"
  },
  {
    label: "Work",
    href: "/works"
  },

  {
    label: "Uses",
    href: "/uses"
  }
]
---

<nav class="flex justify-center items-center gap-2   text-lg font-nunito font-extralight">
  {items.map((item) => (<a class="hover:text-white transition-colors" href={item.href}>{item.label}</a>))}
</nav>
<a class="hover:text-white transition-colors cursor-pointer font-extralight text-lg">GitHub</a>

