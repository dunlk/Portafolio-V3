---
import Navbar from "./Navbar.astro";

interface Props {
  title: string;
}
const { title } = Astro.props;
---

<header class="flex justify-between text-white/85 bg-slate-900/60 px-5 py-2 sticky rounded-xl">
  <h1 class="font-ubuntu font-bold text-xl">{title}</h1>
  <Navbar/>
</header>
